{
  "name": "budget-couple-app",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 migrations apply budget-couple-db --local",
    "d1:migrate:remote": "wrangler d1 migrations apply budget-couple-db --remote",
    "symbols:build": "node scripts/build-symbol-master.js",
    "symbols:sync": "npm run symbols:build && wrangler d1 execute budget-couple-db --remote --file=workers/symbols.sql",
    "symbols:sync:local": "npm run symbols:build && wrangler d1 execute budget-couple-db --local --file=workers/symbols.sql"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.2",
    "vitest": "^2.0.0",
    "wrangler": "^3.78.0"
  },
  "allowScripts": {
    "esbuild@0.17.19": true,
    "esbuild@0.21.5": true,
    "workerd@1.20241230.0": true,
    "workerd@1.20250718.0": true
  }
}
